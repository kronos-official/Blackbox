# فهرست جامع قابلیت‌های Kronos Guard

> **مبنای این فهرست:** بررسی ماژول‌های فعال ربات، فرمان‌های گروه، قرارداد پنل Mini App، سامانهٔ آمار و زمان‌بندی، و قابلیت‌های مدیریتی موجود در نسخهٔ فعلی پروژه. این سند قابلیت‌های واقعاً پیاده‌سازی‌شده را فهرست می‌کند؛ چیزی که صرفاً ایده یا در صف توسعه باشد، به‌عنوان قابلیت فعال معرفی نشده است.

## 1. تصویر کلی محصول

**Kronos Guard** یک ربات مدیریت و امنیت گروه تلگرام به‌همراه Mini App فارسی‌اول است. ربات برای ادارهٔ روزانهٔ گروه، کنترل محتوا، مدیریت مقام‌ها، آمار، عضوگیری اجباری، ضداسپم و ضدحمله طراحی شده است. Mini App نیز محیطی برای مدیران گروه و مالک ربات فراهم می‌کند تا بدون حفظ‌کردن تمام دستورها، تنظیمات و داده‌ها را مشاهده و مدیریت کنند.

| بخش | کار اصلی |
|---|---|
| ربات تلگرام | اجرای فرمان‌های گروه، اعمال فوری قوانین، پاسخ به رویدادهای عضویت و پیام |
| Mini App | کنترل سنتر نقش‌محور برای تنظیمات، اعضا، امنیت، اعلان‌ها، گزارش‌ها و پشتیبانی |
| پایگاه‌داده | نگه‌داری گروه‌ها، اعضا، نقش‌ها، قفل‌ها، فعالیت‌ها، هشدارها، لاگ‌ها و زمان‌بندی‌ها |
| زمان‌بند داخلی | ارسال امن گزارش‌های آمار و پاکسازی/بازنشانی وضعیت‌های منقضی‌شده |

## 2. سطح دسترسی و مدل مقام‌ها

ربات سطح دسترسی فرستنده را با نقش زندهٔ تلگرام و نقش‌های داخلی Kronos بررسی می‌کند. برای عملیات حساس، صرفاً وجود یک رکورد قدیمی در پایگاه‌داده کافی نیست و نقش فعلی تلگرام نیز اعتبارسنجی می‌شود.

| سطح | کاربرد |
|---|---|
| مالک اصلی ربات | کنترل‌های سراسری، پرداخت‌ها، نگه‌داری، اعلان‌های مالک و ممیزی کلان |
| مدیر سراسری | دسترسی مدیریتی سراسریِ تفویض‌شده در ربات |
| مالک گروه / مدیر گروه تلگرام | ادارهٔ گروه و استفاده از ابزارهای مجاز در همان گروه |
| مالک Kronos | نقش داخلیِ سطح‌بالا در همان گروه |
| مدیر / ناظر Kronos | مدیریت محتوا، اعضا و تنظیمات مجاز در همان گروه |
| کاربر ویژه | دریافت معافیت‌ها یا امتیازهای تعریف‌شده در قوانین گروه |
| کاربر عادی | استفاده از ابزارهای عمومی و مشمول قوانین حفاظتی گروه |

**نکتهٔ حفاظتی:** پیام‌های ناشناسِ ادمین که هویت فرستندهٔ آن‌ها برای احراز مقام قابل تشخیص نیست، برای فرمان‌های حساس رد می‌شوند؛ تنها مسیرهای مشخصِ امن برای ادمین ناشناس پشتیبانی شده‌اند.

## 3. فرمان‌های عمومی در گفت‌وگوی خصوصی با ربات

| فرمان / دکمه | قابلیت |
|---|---|
| `/start` | شروع کار با ربات و دسترسی به Mini App |
| `/help` و «راهنمای کامل» | نمایش راهنمای فشردهٔ امکانات و فرمان‌های اصلی |
| `/language` و «زبان رابط» | انتخاب زبان رابط؛ پنل از 12 زبان پشتیبانی می‌کند |
| `/panel` | ورود به کنترل سنتر Mini App |
| `/about` | معرفی محصول و تیم از مسیر Mini App |
| `/support` | ارتباط با پشتیبانی یا ساخت تیکت از Mini App |
| «استخراج آیدی» | دریافت شناسهٔ عددی کاربر، گروه، کانال یا ربات از انتخابگر رسمی Telegram |
| «وضعیت دسترسی» | بررسی زندهٔ وضعیت عضویت اجباری برای دسترسی به سرویس |

### ابزار استخراج آیدی

این ابزار از پنجرهٔ رسمی انتخاب مقصد Telegram استفاده می‌کند، نه جست‌وجوی آزاد یا حدس زدن هویت. دسته‌های **کاربر، ربات، گروه و کانال** دارد، انتخاب را تأیید می‌کند و سپس کارت هویتی شامل شناسهٔ عددی و اطلاعات در دسترس را می‌فرستد. در صورت امکان تصویر پروفایل نیز نمایش داده می‌شود.

## 4. راه‌اندازی و شناخت وضعیت گروه

| فرمان | کارکرد | دسترسی |
|---|---|---|
| `setup` / `راه‌اندازی` | ثبت و فعال‌سازی پایدار گروه در Kronos Guard | مدیر مجاز / نقش مدیریتی Kronos |
| `وضعیت گروه` / `group status` | نمایش وضعیت ثبت گروه، مقام فعلی ربات و تعداد قفل‌های فعال | مدیر مجاز / نقش مدیریتی Kronos |
| `لینک`، `لینک گروه`، `گپ`، `اطلاعات گپ`، `پروفایل گپ`، `مشخصات گپ` | بازکردن مرکز لینک و پروفایل گروه | مدیر یا مالک تلگرام، یا نقش مجاز Kronos |

### مرکز لینک دعوت و پروفایل گروه

مرکز لینک فقط یک لینک ساده نیست. ابتدا کارت گروه شامل نام، نام کاربری، توضیحات، تعداد اعضا، نوع گروه، شناسهٔ عددی، وضعیت ربات و تعداد قفل‌های فعال را آماده می‌کند. اگر تصویر پروفایل گروه موجود باشد، کارت به‌صورت رسانه‌ای با کپشن نمایش داده می‌شود.

| گزینهٔ مرکز لینک | نتیجه |
|---|---|
| نمایش لینک به‌صورت متن | ساخت / نمایش کارت متنی لینک دعوت |
| نمایش لینک به‌صورت عکس | ارسال تصویر پروفایل گروه همراه با کپشن، بیوگرافی و لینک دعوت |
| ساخت لینک یک‌بارمصرف | ساخت لینک با ظرفیت 1 نفر و اعتبار 24 ساعت |
| ساخت لینک درخواست عضویت | ساخت لینک با فعال‌سازی درخواست عضویت |
| اشتراک‌گذاری لینک | بازکردن مسیر رسمی اشتراک‌گذاری Telegram |
| ارسال لینک به پیوی | تحویل امن لینک به گفت‌وگوی خصوصی با deep link دارای انقضای کوتاه |
| ابطال و ساخت تازه | لغو لینک فعلی و ساخت لینک جدید با تأیید لازم |
| راهنمای فعال‌سازی | راهنمای گزینهٔ درخواست عضویت |

## 5. مدیریت عضو و وضعیت فردی

فرمان‌های عضو از **ریپلای روی پیام کاربر**، شناسهٔ عددی و در موارد قابل‌تشخیص نام‌کاربری پشتیبانی می‌کنند. برای هر پیام دربارهٔ یک شخص، نام کاربر به‌صورت منشن تلگرام استفاده می‌شود تا هدف عملیات کاملاً روشن باشد.

| خانوادهٔ فرمان | عملیات |
|---|---|
| وضعیت فرد | `وضعیت کاربر` برای شناسه، یوزرنیم، مقام، اخطار، وضعیت سکوت و تعداد عکس پروفایل |
| پنل فرد | `پنل کاربر` برای پروندهٔ کامل‌تر: لقب Kronos، اولین بازدید، آمار پیام/افزودن عضو در امروز، هفته، ماه و کل، رتبه‌ها، اخطارها و اقدامات مدیریتی |
| محدودسازی | `بن` / `مسدود`، `اخراج`، `سکوت` / `محدود` |
| رفع محدودیت | `رفع بن`، `حذف بن`، `رفع مسدودیت`، `حذف سکوت` |
| اخطار | `اخطار` و `حذف اخطار` با شمارش هشدارهای فعال و اقدام تصاعدی بر پایهٔ سیاست گروه |

سامانه برای عملیات بی‌اثر نیز پاسخ روشن می‌دهد؛ مثلاً اگر شخص در سکوت یا بن نباشد، پیام گمراه‌کنندهٔ موفقیت صادر نمی‌شود. خطاهای رایج Telegram، نبودن دسترسی ربات، هدف نامعتبر و خطای موقت پایگاه‌داده نیز با متن راهنمایی‌شده به مدیر اعلام می‌شوند.

## 6. مقام‌ها، نقش‌های داخلی و لقب‌ها

### نقش‌های داخلی Kronos

| فرمان فارسی | معادل انگلیسی / کاربرد |
|---|---|
| `تنظیم مالک` / `افزودن مالک` | اعطای نقش مالک Kronos |
| `تنظیم مدیر` / `افزودن مدیر` | اعطای نقش مدیر یا ناظر Kronos |
| `تنظیم ویژه` / `افزودن ویژه` | اعطای نقش کاربر ویژه |
| `حذف مالک`، `حذف مدیر`، `حذف ویژه` / `عزل` | حذف یا تنزل نقش داخلی |
| `لیست مالکان`، `لیست مدیران`، `لیست ویژه` | نمایش فهرست نقش‌ها |

ارتقای نقش‌های حساس با **پیام تأیید، دکمهٔ بله/خیر، شمارش معکوس و حذف خودکار در صورت انقضا** انجام می‌شود. از ثبت تکراری نقش جلوگیری می‌شود و اگر کاربر از پیش مقام مساوی یا بالاتر داشته باشد، ربات نتیجهٔ درست را اعلام می‌کند.

### لقب Kronos و تگ مدیریتی

| فرمان | کارکرد |
|---|---|
| `تنظیم لقب` | ثبت لقب داخلی برای کاربر هدف؛ قابل انجام روی کاربر عادی، ویژه، مدیر یا مالک |
| `حذف لقب` | حذف لقب داخلی ثبت‌شده |
| `لقب` | نمایش یا استفاده از لقب ثبت‌شده در مسیرهای پشتیبانی‌شده |

این لقب در پنل کاربر، فهرست اعضای Mini App و جریان‌های مدیریت عضو قابل مشاهده و ویرایش است. تغییر لقب به مقام تلگرام وابسته نیست؛ شرط اصلی، مجازبودن انجام‌دهندهٔ فرمان است.

## 7. قفل محتوا، فیلتر و پاسخ‌های سفارشی

### قفل‌های محتوا

فرمان‌های `قفل …`، `باز کردن …` و `وضعیت قفل` فعال هستند. هر قفل می‌تواند با اقدام **حذف پیام، هشدار یا سکوت موقت** اجرا شود و برای برخی نقش‌ها استثنا داشته باشد.

| انواع قابل مدیریت قفل | انواع قابل مدیریت قفل |
|---|---|
| لینک، عکس، ویدیو، ویس، صوت، استیکر، گیف، سند | فوروارد، منشن، هشتگ، ایموجی، شماره تلفن، موقعیت، نظرسنجی، بازی |
| ربات، فرمان، متن انگلیسی، متن فارسی، پیام ویرایش‌شده | پیام بلند، متن، ریپلای، دکمهٔ درون‌خطی، الفاظ نامناسب، همهٔ محتوا |

Mini App سه الگوی آماده نیز دارد: **فضای باز**، **سپر رسانه** و **نگهبان سخت‌گیر**. پیش از اعمال الگو، وضعیت کنونی قفل‌ها ذخیره می‌شود تا با «بازگردانی وضعیت قبلی» برگشت ایمن ممکن باشد.

### فیلترهای سفارشی

| فرمان | کاربرد |
|---|---|
| `افزودن فیلتر <عبارت>` / `filter add <عبارت>` | ثبت فیلتر واژه با اقدام حذف پیام |
| `حذف فیلتر <شناسه>` / `filter del <شناسه>` | حذف یک فیلتر ثبت‌شده |

فیلترهای فعال روی پیام کاربران غیرمدیریتی اعمال می‌شوند و براساس پیکربندی می‌توانند پیام را حذف، کاربر را موقتاً محدود یا مسدود کنند.

### پاسخ‌ها و فرمان‌های سفارشی

| فرمان | کاربرد |
|---|---|
| `افزودن دستور <تریگر> | <پاسخ>` / `cmd add …` | ساخت یا به‌روزرسانی پاسخ سفارشی گروه |
| `حذف دستور <تریگر>` / `cmd del …` | حذف پاسخ سفارشی |

هنگام ارسال دقیق تریگر توسط کاربر، پاسخ فعالِ ذخیره‌شده از طرف ربات ارسال می‌شود.

## 8. امنیت خودکار: ضداسپم، ضدفلود و کنترل حمله

### ضداسپم و ضدفلود

سیستم ضداسپم برای کاربران غیرمدیریتی نرخ پیام، تکرار متن و تعداد لینک‌ها را بررسی می‌کند. در رخدادهای پرخطر، پیام حذف و کاربر برای بازهٔ موقت محدود می‌شود. رخدادها در لاگ ممیزی ثبت می‌شوند و یک زمانِ آرام‌سازیِ مبتنی بر ریسک نیز اعمال می‌شود.

| کنترل | رفتار |
|---|---|
| پیام‌های پیاپی | مقایسه با سقف پیام در بازهٔ زمانی تنظیم‌شده |
| پیام تکراری | بررسی تعداد تکرار یک متن در بازهٔ تنظیم‌شده |
| پیوندهای پرریسک | دخیل در امتیاز ریسک پیام |
| اقدام | حذف پیام، اعلان حفاظتی و سکوت موقت در صورت عبور از حد |

### کنترل حملهٔ ورود گروهی

| فرمان | رفتار |
|---|---|
| `ضدحمله روشن` / `raid on` | فعال‌سازی محدودسازی موقت اعضای تازه‌وارد برای 15 دقیقه |
| `ضدحمله خاموش` / `raid off` | غیرفعال‌سازی کنترل حمله |
| `ضدحمله وضعیت` / `raid status` | اعلام فعال‌بودن و زمان باقیمانده |

اگر موج ورود مشکوک در کمتر از یک دقیقه تشخیص داده شود، ربات حالت ضدحمله را خودکار فعال کرده، تازه‌واردها را موقتاً محدود می‌کند، رخداد را در ممیزی ثبت می‌نماید و برای مالک هشدار ایجاد می‌کند. بازنشانی حالت منقضی‌شده نیز به‌صورت زمان‌بندی‌شده و idempotent انجام می‌شود.

## 9. عضویت اجباری

سیستم عضویت اجباری می‌تواند پیش از اجازه‌دادن به کاربر برای استفاده از ربات یا Mini App، عضویت او در کانال‌های تعیین‌شده را به‌صورت زنده بررسی کند.

| قابلیت | توضیح |
|---|---|
| چندکاناله | محدودیت شمار کانال در سطح محصول ندارد |
| کارت عضویت | نمایش دکمهٔ ورود به کانال‌های لازم و دکمهٔ بررسی مجدد |
| اعمال در گروه | پیام فردِ تأییدنشده را حذف می‌کند و در صورت نیاز او را محدود می‌سازد |
| بازگشایی خودکار | پس از تأیید عضویت، دسترسی کاربر باز می‌شود |
| Mini App Gate | پیش از ورود به پنل، کانال‌های فاقد عضویت را نشان می‌دهد |
| تحلیل جذب | ثبت نشست‌ها و آمار روزانهٔ مسیر عضویت اجباری |
| پاکسازی انقضا | کانال‌ها یا سفارش‌های موقت منقضی‌شده در چرخهٔ نگه‌داری بررسی می‌شوند |

پیام‌های عضویت اجباری به‌گونه‌ای طراحی شده‌اند که هویت کاربر هدف مشخص باشد و کنترل دکمه‌ها به‌صورت مجاز محدود شود. پیام‌های فوروارد خودکار از کانال لینک‌شده، برای جلوگیری از حذف اشتباه، از این مسیر حفاظتی مستثنا هستند.

## 10. پیام خوشامد و خداحافظی

ورود و خروج عضو می‌تواند پیام خوشامد یا خداحافظی فعال کند. قالب‌ها HTML-ایمن هستند، مقادیر کاربر را escape می‌کنند و نشانه‌گذاری سادهٔ Telegram مانند بولد، ایتالیک، نقل‌قول، کد، اسپویلر، زیرخط و خط‌خورده را پشتیبانی می‌کنند.

| عمل موجود | نمونهٔ فرمان |
|---|---|
| روشن / خاموش‌کردن خوشامد | `welcome on` / `خوشامد روشن` و معادل خاموش |
| روشن / خاموش‌کردن خداحافظی | `goodbye on` / `خداحافظ روشن` و معادل خاموش |
| ثبت متن خوشامد | `welcome text <متن>` / `خوشامد متن <متن>` |
| ثبت متن خداحافظی | `goodbye text <متن>` / `خداحافظ متن <متن>` |

متغیرهای فارسی شامل **منشن کاربر، نام کاربر، آیدی کاربر، آیدی عددی، لینک گروه، قوانین گروه، نام گروه، تاریخ، ساعت، ایموجی خودکار و اجزای تاریخ/زمان** هستند. معادل‌های استاندارد انگلیسیِ آکولادی نیز پشتیبانی می‌شوند.

> **وضعیت فعلی:** ویرایش، فعال/غیرفعال‌سازی و استفادهٔ امن از قالب‌ها فعال است. فرمان‌های «پیش‌نمایش» و «بازنشانی» هنوز در صف توسعهٔ فعلی هستند و در این نسخه قابلیت فعال محسوب نمی‌شوند.

## 11. آمار گروه و رتبه‌بندی

فرمان `آمار` برای مدیران مجاز گروه و نقش‌های داخلی Kronos فعال است. گزارش‌ها فقط از اطلاعاتی ساخته می‌شوند که Kronos Guard واقعاً ثبت کرده باشد و حساب سیستمی Telegram و اعضای نامعتبر از رتبه‌بندی کنار گذاشته می‌شوند.

| گزارش | محتوای اصلی |
|---|---|
| فعالیت امروز | تاریخ و ساعت، پیام‌ها، فوروارد، رسانه‌های تفکیک‌شده، رتبهٔ فعالیت، دعوت عضو و جریان ورود/خروج |
| 30 نفر برتر فعالیت روزانه | بیشترین پیام ثبت‌شده در همان روز |
| فعالیت کاربران مقام‌دار | فعالیت روزانهٔ دارندگان نقش |
| 30 نفر برتر افزودن عضو | رتبه‌بندی افزودن عضو در روز |
| آمار فعالیت هفتگی | پیام، عضو فعال، رسانه، افزودن عضو، ورود/خروج و برترین‌های هفته |
| برترین‌های هر روز هفته | سه عضو برتر برای هر روز از بازهٔ هفت‌روزه |
| 30 نفر برتر فعالیت کل | رتبه‌بندی مجموع فعالیت ثبت‌شده |

آمار پیام شامل شمارش پیام، فوروارد، عکس، ویدیو، ویدیو مسیج، گیف، سند، فایل صوتی، ویس، استیکر و استیکر متحرک است. رتبه‌های معتبر در آمار با منشن واقعی Telegram برای کاربران واجدشرایط نمایش داده می‌شوند.

### زمان‌بندی سفارشی گزارش آمار

از منوی `آمار ← تنظیم زمان‌بندی سفارشی` قابل کنترل است.

| گزینه | جزئیات |
|---|---|
| تناوب | روزانه، هفتگی یا ماهانه |
| ساعت | انتخاب ساعت و دقیقهٔ اجرا |
| روز | روز هفته یا روز ماه، متناسب با تناوب |
| منطقهٔ زمانی | تهران یا UTC در رابط فعلی |
| پیش‌نمایش | نمایش زمان اجرای بعدی پیش از اتکا به برنامه |
| فعال / غیرفعال | روشن یا خاموش‌کردن ارسال خودکار |

ارسال خودکار از زمان‌بند داخلی انجام می‌شود، نیازی به API پولی ندارد و با **کلید تحویل یکتا** از ارسال تکراری گزارش برای یک گروه، تناوب و تاریخ جلوگیری می‌کند. وضعیت موفق، ناموفق یا ردشدن ایمنِ هر تحویل ثبت می‌شود.

## 12. ابزار «تگ» و اطلاع‌رسانی کنترل‌شده

فرمان `تگ` یک جریان انتخابی برای اطلاع‌رسانی گروهیِ کنترل‌شده دارد. مدیر روش را از دکمه‌ها انتخاب می‌کند، عملیات بعد از انتخاب اجرا می‌شود، پیام انتخاب پس از انجام پاکسازی می‌گردد و برای جلوگیری از هرزنگاری، زمان‌سنج و محدودیت اجرا دارد. دکمه‌های انتخاب روش و لغو نیز با وضعیت بصری جدا طراحی شده‌اند.

## 13. Mini App: بخش‌های در دسترس مدیران گروه

Mini App با احراز هویت Telegram Mini App کار می‌کند، وضعیت عضویت اجباری را پیش از ورود بررسی می‌کند، RTL و زبان فارسی را به‌صورت پیش‌فرض در نظر می‌گیرد و در کنار فارسی از 11 زبان دیگر پشتیبانی دارد.

| تب | امکانات |
|---|---|
| گروه‌ها | دیدن گروه‌های متصل و ورود به فضای مدیریت گروه انتخاب‌شده |
| اعضا | فهرست اعضا، اطلاعات عضو، لقب Kronos، نقش داخلی، نقش تلگرام و همگام‌سازی مقام‌ها |
| مدیریت محتوا | قفل‌های محتوا، نوع اقدام، معافیت نقش‌ها و الگوهای آمادهٔ سیاست قفل |
| سیاست اخطار | سقف اخطار، اقدام بعد از سقف، مدت سکوت و سیاست‌های مرتبط |
| عضویت اجباری | کانال‌های الزامی، وضعیت کاربر، مقصدها و داده‌های تحلیل عضوگیری |
| اعلان‌ها | صندوق اعلان فردی، نشانگر تعداد خوانده‌نشده، فیلتر بر اساس گروه و نوع رویداد، خواندن تکی یا همه |
| پشتیبانی | ثبت تیکت، پیگیری تیکت، پاسخ، پیوست مجاز و کنترل وضعیت طبق دسترسی |
| راهنما | راهنمای تفصیلی امکانات و فرمان‌ها در رابط Mini App |
| دربارهٔ ما | معرفی محصول و اطلاعات عمومی |

### مدیریت تنظیمات گروه از Mini App

تنظیمات زیر به‌صورت فرمی در پنل گروه وجود دارد: پیام ورود و خروج و متن آن‌ها، قوانین گروه، ضداسپم، ضدحمله، سقف فلود، بازهٔ فلود، سقف تکرار، سقف اخطار، اقدام اخطار و مدت سکوت. تغییرها با بازخورد درون‌برنامه‌ای ثبت می‌شوند و رخدادهای مهم می‌توانند اعلان تولید کنند.

### اعضا و مقام‌ها در Mini App

مدیر مجاز می‌تواند فهرست اعضا را مرور کند، لقب داخلی را ببیند یا ویرایش کند، نقش داخلی Kronos را تغییر دهد، در محدودهٔ مجاز نقش واقعی تلگرام را مدیریت کند و دادهٔ مقام مدیران تلگرام را تازه‌سازی کند. تغییر مقام به کاربر هدف و انجام‌دهنده با اعلان و منشن مناسب گزارش می‌شود.

## 14. Mini App: امکانات اختصاصی مالک اصلی ربات

این بخش‌ها فقط برای مالک اصلی Kronos Guard در دسترس هستند.

| تب / ابزار | کارکرد |
|---|---|
| نمای کلی | شاخص‌های کلان، نمای عملیاتی و گروه‌های متصل |
| ثبت گروه‌ها | مشاهده و مدیریت رجیستری گروه‌های متصل |
| پرداخت‌ها و بازارچه | تنظیمات قیمت/ظرفیت، فاکتور Stars، سفارش، رسید، تأیید یا رد پرداخت دستی |
| اعلان‌های مالک | هشدارهای عملیاتی، امنیتی و رخدادهای حساس |
| تنظیمات سراسری | تنظیمات مدیریتیِ مالک و نگه‌داری محصول |
| مرکز ممیزی | جست‌وجوی رخدادهای گروه بر اساس گروه، شناسهٔ عامل، عمل، نتیجه و بازهٔ تاریخ |
| نسخه‌های سیاست گروه | پیش‌نمایش تغییر، ثبت نسخهٔ تغییرناپذیر، تاریخچه و بازگردانی امن سیاست همان گروه |
| پیام سراسری | آماده‌سازی و ارسال ارتباطات سراسری از محیط مالک |
| نگه‌داری | ابزارهای کنترل عملیاتی، همگام‌سازی و عملیات حساس با محافظت مناسب |
| بازنشانی پایگاه‌داده | عملیات بسیار حساسِ مالک‌محور با عبارت تأیید صریح |

### مرکز ممیزی و سیاست نسخه‌بندی‌شده

مرکز ممیزی رخدادها را به شکل group-scoped نگه می‌دارد. فیلترهای آن شامل گروه، شناسهٔ عددی عامل، نوع عملیات، نتیجهٔ `allowed / denied / completed / failed` و بازهٔ زمانی است. سیاست‌های گروه نیز نسخه‌گذاری تغییرناپذیر دارند: ابتدا خلاصهٔ تغییر پیش‌نمایش می‌شود، سپس نسخه ثبت می‌شود و بازگردانی فقط از نسخهٔ متعلق به همان گروه و سیاست امکان‌پذیر است.

## 15. اعلان‌ها و پشتیبانی

| بخش | قابلیت |
|---|---|
| اعلان کاربر | اعلان رویدادهای مهم گروه و تغییر مقام |
| نشانگر خوانده‌نشده | نمایش تعداد پیام‌های جدید در نوار اصلی Mini App |
| فیلتر اعلان | محدودسازی لیست بر اساس گروه و نوع رخداد |
| خواندن اعلان | خواندن تکی یا خواندن همه با حالت درحال‌انجام و بازخورد موفقیت |
| تیکت پشتیبانی | ساخت، مشاهده و پاسخ به تیکت |
| کنترل تیکت | سطح اولویت و دسته، وضعیت، پیوست‌های معتبر، و دسترسی مالک یا درخواست‌کننده |

برای هر کاربر حداکثر دو تیکت فعال هم‌زمان پذیرفته می‌شود تا مرکز پشتیبانی قابل مدیریت بماند.

## 16. رویدادهای خودکار و چرخهٔ نگه‌داری

| رویداد | واکنش خودکار |
|---|---|
| ورود عضو | ثبت رویداد، محاسبهٔ جریان اعضا، اجرای خوشامد، کنترل عضویت اجباری و تشخیص موج ورود |
| خروج عضو | ثبت جریان خروج و اجرای خداحافظی در صورت فعال‌بودن |
| پیام جدید / ویرایش‌شده | اجرای زنجیرهٔ ایمنی: پیکربندی مجاز، ضدفلود، پاسخ سفارشی، قفل‌ها و فیلترها |
| موج ورود مشکوک | محدودسازی تازه‌واردان، ثبت ممیزی و هشدار مالک |
| انقضای ضدحمله | بازنشانی امن و idempotent در چرخهٔ نگه‌داری |
| زمان گزارش آمار | ارسال گزارش فقط در صورت فعال‌بودن برنامه و معتبر بودن گروه و task |
| تحویل گزارش تکراری | رد ایمن، بدون ارسال دوباره |
| انقضای موارد موقت عضویت اجباری | بررسی و پاکسازی در چرخهٔ زمان‌بندی‌شده |

## 17. مواردی که هنوز «فعال» نیستند

برای شفافیت کامل، این دو قابلیت در کار فعلی شناسایی و طراحی شده‌اند، اما هنوز به فرمان‌های فعال اضافه نشده‌اند:

| قابلیت در صف | وضعیت |
|---|---|
| `welcome preview` / `خوشامد پیش‌نمایش` و همتای خداحافظی | هنوز پیاده‌سازی نشده |
| `welcome reset` / `خوشامد بازنشانی` و همتای خداحافظی | هنوز پیاده‌سازی نشده |

پس از تکمیل، این دو گزینه به سامانهٔ موجود قالب‌های خوشامد و خداحافظی اضافه خواهند شد؛ پیش‌نمایش بدون ذخیره‌سازی، متن نمونه را نشان خواهد داد و بازنشانی قالب سفارشی را پاک می‌کند تا متن پیش‌فرض داخلی دوباره استفاده شود.

## 18. جمع‌بندی سریع برای استفادهٔ روزمره

| اگر می‌خواهید… | از این بخش استفاده کنید |
|---|---|
| گروه را فعال و امن کنید | `setup`، قفل‌ها، ضداسپم، ضدحمله و عضویت اجباری |
| یک عضو را مدیریت کنید | ریپلای روی پیام او و فرمان‌های بن/سکوت/اخطار/وضعیت/پنل کاربر |
| تیم مدیریتی داخلی بسازید | تنظیم مالک، مدیر، ویژه و لقب Kronos |
| لینک حرفه‌ای دعوت بسازید | `لینک` و مرکز متن/عکس/یک‌بارمصرف/درخواست عضویت |
| فعالیت گروه را ببینید | `آمار` و منوی گزارش‌های روزانه، هفتگی و رتبه‌بندی |
| گزارش را خودکار کنید | `آمار ← تنظیم زمان‌بندی سفارشی` |
| بدون فرمان کار کنید | Mini App: گروه‌ها، اعضا، امنیت، عضویت اجباری، اعلان‌ها و پشتیبانی |
| سابقه و سیاست‌ها را کنترل کنید | مرکز ممیزی و نسخه‌های سیاست؛ فقط برای مالک اصلی |
