# Kronos Guard — نقشهٔ راه و ممیزی قابلیت‌ها

## دامنه و روش

این سند بر اساس ثبت‌های فعال در `server/telegram/bot.ts`، ماژول‌های Telegram موجود، مدل‌های `drizzle/schema.ts` و آزمون‌های رگرسیون فعلی تهیه شده است. هدف آن تفکیک قابلیت‌های واقعاً موجود از ایده‌های توسعه‌ای است؛ هیچ قابلیتِ صرفاً پیشنهادی به‌عنوان قابلیت آماده معرفی نشده است.

## قابلیت‌های موجود و قابل اتکا

| حوزه | وضعیت فعلی | شواهد اجرایی |
|---|---|---|
| دسترسی و نقش‌ها | فعال؛ نقش‌های داخلی Kronos برای فرمان‌های تحت‌کنترل ربات، با حفظ مرز واگذاری مقام | `authorization.ts`, `rolePolicy.ts`, `roleManagement.ts` |
| لینک گروه | فعال؛ لینک متنی، تصویری، یک‌بارمصرف، درخواست عضویت، ارسال خصوصی و callbackهای محافظت‌شده | `groupInfo.ts`, `groupInfo.access.test.ts` |
| آمار | فعال؛ مسیر پیام و callback با کنترل دسترسی داخلی | `statistics.ts`, `statistics.access.test.ts` |
| قفل و ایمنی گروه | فعال؛ قفل‌های محتوا، تنظیمات ایمنی و کنترل عضویت اجباری | `locks.ts`, `groupSafety.ts`, `forcedJoin.ts` |
| مدیریت عضو | فعال؛ سکوت، بن، رفع محدودیت، لقب داخلی، تأیید ارتقای نقش و audit | `moderation.ts`, `administratorTitles.ts`, `roleManagement.ts` |
| تگ و اعلان | فعال؛ انتخاب گروه هدف، محدودیت سرعت، زمان انقضا و کنترل‌های مرحله‌ای | `groupTagging.ts`, `alerts.ts` |
| هویت و اطلاعات | فعال؛ استخراج شناسهٔ عددی، پنل کاربر، وضعیت کاربر و cache متادیتای گروه | `numericIdConversion.ts`, `userPanel.test.ts`, `userStatus.test.ts` |
| Mini App و چندزبانه | فعال؛ کنترل سنتر مالک، راهنما، اعلان‌ها، آمار، گروه‌ها و ۱۲ زبان | `client/`, `dashboardCommandGuideI18n.ts` |
| پشتیبانی و عملیات | فعال؛ تیکت، webhook security، retry، نگهداری زمان‌بندی‌شده و status endpoint | `server/dashboard/`, `webhookSecurity.ts`, `scheduledMaintenance.ts`, `routes.ts` |

## اولویت‌های توسعهٔ بعدی

| اولویت | قابلیت پیشنهادی | ارزش عملیاتی | ریسک و وابستگی |
|---:|---|---|---|
| P0 | مرکز رویداد و audit قابل جست‌وجو برای هر گروه، با فیلتر actor، نوع اقدام و بازهٔ زمانی | تشخیص خطا، پاسخ‌گویی و امنیت | نیازمند طراحی query و index؛ دادهٔ حساس باید حداقلی بماند |
| P0 | موتور policy قابل‌تنظیم برای استثناهای نقش، command و گروه | جلوگیری از ناهم‌خوانی مجوزها | نیازمند schema و تست ماتریس نقش؛ نباید مرز Telegram API را جعل کند |
| P1 | ضداسپم چندمرحله‌ای با امتیاز ریسک، cooldown و حالت raid | کاهش اقدامات دستی مدیر | نیازمند محدودیت نرخ، observability و آزمون بار |
| P1 | زمان‌بندی عملیاتی برای پاکسازی، گزارش و بازنشانی قفل‌ها | کاهش کار روزانه مدیر | نیازمند Heartbeat و سیاست retention؛ اجرای پس‌زمینه باید idempotent باشد |
| P1 | قالب‌های پیام قابل شخصی‌سازی با preview و fallback امن HTML | تجربهٔ بومی‌تر گروه | باید escaping و fallback Unicode حفظ شود |
| P2 | گزارش‌های قابل خروجی برای مدیر با بازهٔ روزانه، هفتگی و ماهانه | تحلیل رشد و سلامت گروه | نیازمند تعیین schema خروجی و محدودیت حجم |
| P2 | مرکز سلامت ربات برای webhook، تأخیر API، خطا و cache hit/miss | پایش production | بخشی از داده‌ها فقط باید برای مالک قابل مشاهده باشد |
| P3 | سیستم rule automation برای واکنش به رویدادهای گروه | اتوماسیون پیشرفته | سطح ریسک و نیاز به sandbox بالا است؛ بعد از policy engine اجرا شود |

## تصمیم معماری

مرحلهٔ بعدی باید از **audit جست‌وجوپذیر و policy engine** آغاز شود، نه افزودن فرمان‌های پراکنده. هر قابلیت جدید باید چهار لایه داشته باشد: مدل دادهٔ صریح، helper دسترسی مرکزی، handler پیام و callback هم‌راستا، و آزمون ماتریس نقش. متن‌ها باید فارسی‌اول و RTL باشند و اعداد خروجی ربات به رقم انگلیسی نرمال شوند.

## مواردی که عمداً در این مرحله اجرا نشده‌اند

ارسال Custom Emoji Premium به محدودیت خود Telegram Bot API وابسته است و با کد صرفاً سمت ربات برای مالک غیرPremium قابل تضمین نیست. همچنین عملیات حساس مانند واگذاری مقام، تغییر مالکیت و مجوزهای native Telegram نباید فقط با نقش داخلی Kronos شبیه‌سازی شوند.

## معیار پذیرش مرحلهٔ بعد

پیش از هر انتشار جدید، مسیر پیام و callback با حداقل نقش `moderator`، نقش `user`، مدیر native Telegram و مالک گروه آزموده می‌شود؛ `pnpm test`، `pnpm check` و `pnpm build` باید موفق باشند؛ و endpoint وضعیت باید revision استقرار را نمایش دهد.
