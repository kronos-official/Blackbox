# گزارش تحویل نهایی Kronos Guard

## خلاصهٔ اجرایی

نسخهٔ جاری **Kronos Guard** یک پلتفرم فارسی‌اول برای مدیریت حرفه‌ای گروه‌های تلگرام است. این نسخه شامل ربات Telegraf، Mini App مدیریتی، کنترل‌های moderation و content-lock، forced-join، مدیریت لینک، مبدل آیدی عددی، بازارچهٔ مبتنی بر Telegram Stars، پشتیبانی تیکتی، اعلان‌های پایدار، کنترل دسترسی چندلایه و پوشش ۱۲ زبان است.

تمرکز این مرحله بر بازطراحی مرکز ساخت لینک و مبدل آیدی عددی، همراه با حذف ویرایش‌های تکراری پیام Telegram بود. متن آغازین مرکز لینک کوتاه شد، مسیر تصویر با عنوان «نمایش لینک به صورت عکس» و لینک دعوت تصادفی تنظیم شد، مبدل آیدی به فهرست انتخابی مقصدهای شناخته‌شده تغییر کرد و بررسی برابری متن/markup پیش از edit افزوده شد. نتیجهٔ role-scope پیشین نیز بدون تغییر معتبر باقی است: **نقش داخلی Moderator به‌تنهایی دسترسی گروه را در فهرست Mini App ایجاد نمی‌کند.**

## وضعیت قابلیت‌های اصلی

| حوزه | وضعیت | توضیح و مبنای بررسی |
|---|---|---|
| ورود Mini App از `/start` و Chat Menu Button | تکمیل‌شده | مسیر پاسخ خصوصی و تنظیم خودکار Chat Menu Button پیاده‌سازی و آزمون شده است. |
| moderation و حفاظت محتوا | تکمیل‌شده در کد و آزمون | ban، mute، warn، unban، unmute، قفل رسانه، ضداسپم، ضدraid و پاک‌سازی کنترل‌شده پوشش داده شده‌اند. |
| forced-join | تکمیل‌شده | بررسی عضویت، قفل زنده، پیام راهنما و بازبینی عضویت پیاده‌سازی شده است. |
| مدیریت لینک | تکمیل‌شده در کد و آزمون | متن آغازین کوتاه، لینک متنی، نمایش لینک به صورت عکس، تصویر پروفایل، لینک دعوت تصادفی حتی برای چت دارای Username، یک‌بارمصرف، درخواست عضویت، اشتراک‌گذاری، ابطال و ساخت مجدد، راهنما و بستن در منوی عمودی اجرا شده‌اند. |
| مبدل آیدی عددی | تکمیل‌شده در کد و آزمون | ورود دستی حذف شده و انتخاب‌گر مقصدهای شناخته‌شدهٔ گروه/کانال/کاربر/ربات، اطلاعات مقصد، کادر مستقل آیدی عددی و دکمهٔ کپی وجود دارد. |
| پشتیبانی تیکتی | تکمیل‌شده | وضعیت‌ها، فیلتر، پیوست، نمایش زمان، پیام‌های تفکیک‌شدهٔ کاربر و مدیر، قفل حالت بسته و اعلان Telegram پیاده‌سازی شده است. |
| اعلان پاسخ تیکت | تأییدشدهٔ زنده | پاسخ واقعی از پنل مالک، marker پایدار `notification:delivered` و مشاهدهٔ اعلان در حساب دوم ثبت شده است. |
| نقش‌های داخلی Kronos | پیاده‌سازی‌شده و آزمون‌شده | مالک، Moderator، VIP و کاربر عادی در policy و تست‌های دسترسی پوشش داده شده‌اند؛ این نقش‌ها از نقش‌های Telegram-native جدا هستند. |
| نمایش گروه در Mini App | عمداً محدود به دسترسی زندهٔ Telegram | گروه‌های کاربر غیرمالک فقط هنگامی نمایش داده می‌شوند که Telegram همان حساب را owner یا administrator معتبر تشخیص دهد. نقش داخلی Kronos به‌تنهایی این دسترسی را ایجاد نمی‌کند. |

## دروازهٔ کیفیت

آخرین اجرای کامل کیفیت شامل **۷۰ فایل تست و ۲۷۱ تست موفق** بود. بررسی TypeScript با `--noEmit` بدون خطا پایان یافت و build تولیدی Vite و esbuild نیز موفق شد. هشدار اندازهٔ chunk اصلی و هشدار deprecation مربوط به `punycode` غیرمسدودکننده بودند و مانع تولید خروجی نشدند.

خطای Telegram با متن `400: Bad Request: message is not modified` نیز به‌صورت idempotent مدیریت شده است: اگر متن/کپشن و reply markup برابر باشند، درخواست edit ارسال نمی‌شود؛ guard قبلی برای no-op واقعی Telegram نیز حفظ شده است. خطای Telegram با متن `400: Bad Request: can't use file of type ChatPhoto` نیز اصلاح شده است. مسیر کارت تصویری اکنون فقط شناسهٔ فایل رشته‌ای معتبر را عبور می‌دهد و برای ورودی نامعتبر به fallback متنی بازمی‌گردد؛ regression test مربوط در مجموعهٔ آزمون‌ها قرار دارد.

## پذیرش زندهٔ اعلان پشتیبانی

در آزمون واقعی، mutation رسمی پاسخ تیکت برای تیکت `KG-T-74UB4JYBHW` از نشست مالک با شناسهٔ `8375579910` اجرا شد. API مقدار `success=true` و `notificationDelivered=true` را برگرداند، وضعیت تیکت به `waiting_user` تغییر کرد، رویداد durable با marker `notification:delivered` ثبت شد و پیام اعلان در چت خصوصی حساب دوم با شناسهٔ `8618912202` در Telegram Web مشاهده شد.

این مسیر علاوه بر پاسخ متنی، نتیجهٔ تحویل اعلان را در رویدادهای پایدار `notification:delivered`، `notification:failed` و `notification:not_applicable` ثبت می‌کند. بنابراین نتیجهٔ ارسال پس از پایان mutation نیز برای ممیزی باقی می‌ماند.

## آزمون کنترل‌شدهٔ role-scope

آزمون با شناسه‌های زیر انجام شد:

| مورد | مقدار |
|---|---:|
| مالک ربات | `8375579910` |
| حساب دوم | `8618912202` |
| گروه آزمایشی | `-1003795743979` |
| شناسهٔ داخلی گروه در پایگاه‌داده | `13080002` |
| نقش آزمون | `moderator` |

از مسیر رسمی `setKronosRole`، نقش داخلی Moderator برای حساب دوم در گروه آزمایشی ثبت شد و وجود ردیف آن در `groupRoles` تأیید گردید. سپس Telegram Web با نشست مستقل `?account=2` باز شد و payload مربوط به Mini App شناسهٔ حساب دوم `8618912202` و username `justimmortalman` را نشان داد؛ بنابراین هویت نشست مستقل بود.

در داشبورد حساب دوم، کارت گروه «باع» با وضعیت «مالک ربات» نمایش داده شد، اما گروه آزمایشی Anonymous با شناسهٔ `-1003795743979` در فهرست قرار نگرفت. این نتیجه با منطق فعلی سیستم سازگار است: فهرست گروه‌های کاربر غیرمالک بر دسترسی زندهٔ Telegram owner/administrator متکی است و نقش داخلی Kronos Moderator صرفاً مجوزهای داخلی Kronos را کنترل می‌کند؛ این نقش به‌تنهایی دسترسی Telegram-native یا نمایش گروه را اعطا نمی‌کند.

پس از پایان شاهدگیری، همان ردیف آزمایشی با شرط دقیق `groupId=13080002`، `telegramUserId=8618912202` و `role=moderator` حذف شد. query تأییدی پس از cleanup هیچ ردیفی برای این ترکیب برنگرداند. هیچ نقش Telegram-native، تنظیم گروه، پرداخت یا دادهٔ عملیاتی دیگری تغییر نکرد.

> نتیجهٔ پذیرش: ثبت و بازگردانی نقش داخلی موفق و قابل‌ردیابی بود؛ اما فرض «نمایش گروه فقط با Moderator داخلی» در معماری فعلی تأیید نشد. این موضوع نقص پنهان یا موفقیت ادعایی محسوب نمی‌شود، بلکه مرز آگاهانهٔ بین نقش داخلی Kronos و دسترسی زندهٔ Telegram است.

## وضعیت پذیرش‌های باقی‌مانده

| مورد | وضعیت فعلی | توضیح دقیق |
|---|---|---|
| شواهد مستقل همهٔ سطوح owner، admin، moderator و VIP | محدودیت مستندشده | owner و Moderator با نشست‌های مستقل بررسی شدند؛ نشست جداگانهٔ Admin/VIP در اختیار نبود و این عدم‌دسترسی به‌عنوان محدودیت شاهد ثبت شده است. policy و regressionهای هر چهار سطح سبز هستند. |
| آزمون actor-guard برای update بدون `from` | تأییدشدهٔ خودکار | helper واقعی به مسیر Telegraf متصل و regression قطعی برای رد فرمان ممتاز بدون actor اضافه شد؛ تولید چنین update ناقصی از کلاینت عادی Telegram ممکن نیست. |
| قفل رسانه در گروه واقعی | تأییدشدهٔ زنده | پیام `76919` فقط در گروه `-1003795743979` ارسال شد و پس از چند ثانیه در Telegram Web وجود نداشت؛ حذف فوری ثبت شد. |
| invoice خصوصی Stars | مشروط و باز | فقط در صورت وجود مقصد معتبر و دسترسی مدیریتی، بدون شارژ یا تغییر تنظیمات تولید. |

موارد بسته‌شدهٔ این مرحله شامل اعلان واقعی پاسخ تیکت، ثبت پایدار تحویل اعلان، اصلاح ChatPhoto، آزمون کنترل‌شده و بازگردانی نقش، regression actor-guard، و حذف زندهٔ رسانه در گروه مجاز است. تنها محدودیت باقی‌مانده، نبود نشست مستقل Admin/VIP و invoice خصوصیِ دارای مقصد معتبر است؛ هیچ‌کدام به‌عنوان موفقیت ادعا نشده‌اند.

## فایل‌ها و شواهد کلیدی

گزارش جزئیات تعاملات زنده در `notes/live_browser_evidence_2026-08-16.txt` قرار دارد. فهرست کامل کارها در `todo.md` نگهداری می‌شود. قرارداد نقش و اعضا در `server/dashboard/router.members.ts` و مسیرهای پشتیبانی در `server/dashboard/supportRouter.ts` قرار دارند. تست‌های مرتبط شامل `server/dashboard/supportRouter.test.ts`، `server/dashboard/router.members.test.ts`، `server/dashboard/liveTelegramRoles.test.ts` و تست‌های `groupInfo` و اعلان Telegram هستند.

دامنهٔ انتشار پروژه:

> https://kronosmod-nul7benn.manus.space

ربات رسمی:

> https://t.me/kronosguard_bot

## مراجع داخلی

[1]: `notes/live_browser_evidence_2026-08-16.txt` — گزارش شواهد زندهٔ Telegram Web و Mini App.
[2]: `todo.md` — فهرست کنترل پروژه و وضعیت موارد پذیرش.
[3]: `server/dashboard/router.members.ts` — قرارداد رسمی مدیریت نقش‌های داخلی.
[4]: `server/dashboard/supportRouter.ts` — مسیر پاسخ تیکت و وضعیت تحویل اعلان.
[5]: `server/dashboard/supportRouter.test.ts` — regressionهای پشتیبانی و اعلان.
