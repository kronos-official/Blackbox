# Secrets policy

Never commit `.env`, project metadata, database dumps, bot tokens, API keys, session cookies, or provider secrets. Configure required values only in RamNaym's encrypted Environment Variables/Secrets panel. The committed environment templates contain placeholders only.
