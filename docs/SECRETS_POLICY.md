# Secrets policy

Never commit `.env`, project metadata, database dumps, bot tokens, API keys, session cookies, or provider secrets. Configure required values only in the hosting provider's encrypted Environment Variables/Secrets panel. The committed templates contain placeholders only.
