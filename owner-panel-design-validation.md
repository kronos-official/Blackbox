# Owner Control Center — Motion Validation

بازبینی ۲۷ اوت ۲۰۲۶ در دو viewport انجام شد. فرم ورود در دسکتاپ و موبایل بدون overflow نمایش داده شد، هویت جدید Kronos با نماد سپر K و حلقهٔ مداری خوانا است، و پس‌زمینهٔ واکنشی بدون کاهش کنتراست متن یا فیلدهای ورود باقی می‌ماند. انیمیشن‌ها فقط transform و opacity را تغییر می‌دهند و برای `prefers-reduced-motion` غیرفعال می‌شوند.

| Viewport | نتیجه |
| --- | --- |
| 1280 × 720 | کارت ورود، نماد، ورودی‌ها و CTA همگی خوانا و در محدودهٔ viewport هستند. |
| 390 × 844 | فرم کامل، قابل لمس و بدون برش افقی یا عمودی نمایش داده می‌شود. |
